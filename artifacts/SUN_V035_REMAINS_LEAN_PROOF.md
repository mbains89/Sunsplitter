SOURCE main@8d23109 · RUNTIME b80a9e86 · TASK SUN-V035-PLAYTEST-REMAINS-LEAN-01 · MODE implementation

Build / launch node, sole isolated writer. Owner-authorized leftover drain
after PR 138. One PR into `version/0.30.1-main-reconcile-ci.1`; stop for
owner review. **NO-PUBLISH / NOT_CERTIFIED**; last certified remains 0.28.1d.

## Authority and reproduction

- Main authority: `8d23109b63b844e0703fb36643f14b91b8800c90`.
- Exact predecessor / write-lane HEAD: `b80a9e8607a0f2a9016864c2aaf96ebc35b37cfd`
  (owner merge of PR 138). Hosted v0.33 is not this HEAD.
- AGENTS, ROADMAP, PROJECT_STATUS, LOCKS read from main@8d23109 in this
  session. Lane STATUS is newer than the stale main copy; no authority
  file was rewritten.
- PLAYTEST_SUN pass 4 classified WHAT REMAINS as split on a Future-leaning
  path. The labeled hunch — early vault crisis unavailable at full Future
  by one cohesion — was verified on these bytes before any repair.

## Confirmed defect, not ALREADY_SATISFIED

Actual rendered buttons on `b80a9e86`:

1. `vault_reveal` Future priority (`future +6`).
2. Strict rationing (`future +2`).
3. Jump to `vault_sacrifice` with cohesion forced to 11.
4. “Divert everything to the vault” is disabled: Needs 12 Cohesion; 11
   available. Split remains the affordable Future-adjacent exit.
5. Split writes `vault_sacrifice: split` and `future 11 / living 3`.

Recorded-order threshold is `future - living >= 8`. Weights are Future.
`ideologyShape()` still returns `split` because the vault flag overrides
endings. WHAT REMAINS first line nevertheless said “The recorded orders
remained split between Future and Living.”

The hunch that a mixed vault exit already matched the split ledger is
false on this path: weights are Future-leaning, the ledger was not.

Vault outcome remains a separate later fact (“At the vault fault, both
habitation and the restart package were degraded.”).

## Bounded repair

WHAT REMAINS ideology line now uses recorded-order weights only.

`ideologyShape()` is unchanged. Endings, destinations, costs, relationship
state, Continue, and Import are unchanged. No ART-R2 plate is retargeted.

V5 `what_remains_ideology_disagrees_with_totals` now compares the first
WHAT REMAINS line to those same totals. It no longer treats a split or
Living vault flag as the recorded-order lean.

## Files

- `src/state.js` — weight-only WHAT REMAINS lean helper.
- `scripts/remains-lean-checks.mjs` — named-path, fixture, ART-R2, and
  Continue/import regressions.
- `scripts/verify.mjs` — wire checks; add the weight-true fixture to the
  existing What Remains suite.
- `scripts/simulate.mjs` — V5 totals alignment plus self-test.
- this proof.

## Limits and stop

Actual-engine / browser-stub evidence with a controlled vault-cohesion
context. Not a fresh full phone or hosted playtest. No deployed-byte or
certification claim.

L-002 / L-008 / L-021 / L-022 / L-025 unchanged. ART-R2 held as already
landed plates. Amara-route parked. No remint 107–138, SEALANT leftover,
Ashes, 0.36, opening backstory, ending rewrite, merge, tag, or deploy.

Next action: Manraj reviews the one PR and, if accepted, merge-commits in
the browser, not squash. Launch stops and will not merge.

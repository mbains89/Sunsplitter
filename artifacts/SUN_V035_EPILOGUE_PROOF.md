SOURCE main@8d23109 · RUNTIME bc1f021 · TASK SUN-V035-PLAYTEST-EPILOGUE-01 · MODE implementation

Build / $ Con V9, sole isolated writer. Owner explicitly authorized the launch
in `0011CT-V9-PLAYTEST-EPILOGUE-01.md`: one PR to
`version/0.30.1-main-reconcile-ci.1`, then stop for review.

## Source and confirmed reproduction

- Main authority: `8d23109b63b844e0703fb36643f14b91b8800c90`.
- Exact predecessor: `bc1f021dbf1cdda884848bd61e748766f939f68a`, verified
  owner merge of PR 125. Its tree matches the reviewed capacitor candidate.
- Read global/repository AGENTS, main/lane ROADMAP, PROJECT_STATUS, LOCKS,
  SCENE_SKELETON and GITHUB_PUSH_RULES in this continuous session; refetched
  main/lane and verified those authority/specialist files unchanged. The
  explicit newer runtime pin controls, not historical STATUS.
- Read current inbox, PLAYTEST_SUN finding 3 and NEXT_AFTER_SELA. The latter's
  historical PR 125 open state is superseded by the live merge readback.
- On exact predecessor, a controlled completed-early-Lena/Amara context shows
  the original recap at `debt_notice`; its free onward choice reaches
  `act2_tether_sighting`. After admitting recovered Tomas/Jiro, actual rendered
  buttons perform Vess boarding/acceptance/window/private hour, the shared
  Amara/Tomas encounter, Amara's negotiated second encounter and promise return.
- On that later `debt_notice`, the original four relationship paragraphs still
  omit Vess and the shared encounter, although their persistent flags are set.
  The menu already updates and onward Continue works. No hard loop is claimed.

## Narrow repair and factual sources

Only `debt_notice.text` changes. It now reads the current saved relationship
facts on each render and adds:

- Living Vess with an accepted attempt: the existing accepted-attempt sentence
  from `reckon_summary` in `src/scenes-27.js`.
- Living Vess with a completed private hour: the exact existing memory sentence
  from `vess_intimate` in `src/scenes-18.js`, requiring both `romance.vess`
  and `flags.vess_intimate`.
- The shared bay: the first sentence of the existing encounter memory in
  `src/scenes-31.js`, requiring `romance.amara_tomas` and both Amara and Tomas
  alive/recovered.

These sentences are copied as prose, not read from the bounded memory list.
An empty/evicted memory list cannot erase a recorded relationship. An accepted
Vess offer is not treated as physical intimacy. Group participation does not
invent solo Amara romance or new knowledge for the original four speakers.

Original-four dialogue/mirrors, relationship-debt text, both exit descriptors,
encounter costs/rewards, used-choice gates, flags, state/schema, art, engine,
and every other scene remain unchanged. No new scene, helper or state field.

## Targeted proof

- Regression fails on the predecessor's later recap and missing group fact.
- The real-button later route now includes both missing facts. Its existing
  encounter payments remain Supplies -2 / Cohesion +4 across the tested route.
- Two post-encounter Continues reset live state then restore exact saved state
  and bytes; repeated rendering changes no rewards, flags or choices.
- Used Vess, shared encounter and second-Amara offers remain unavailable;
  the existing free onward choice reaches `act3_spine_next`.
- Forty-eight acceptance/completion/group/live/dead/unrecovered combinations
  distinguish missing, declined, accepted-only and completed Vess facts,
  group-only participation, and absent partners. All use empty memories.
- Actual Vess decline, accepted-but-window-refused and group-privacy choices
  do not fabricate completed encounters.
- Twenty-four real imports x two Continues cover early/accepted/completed/
  absent contexts, all three existing recovery-dependent exit destinations,
  and current/marker-less saves. Exact history/view and both exit descriptors
  survive; current bytes remain exact and legacy one-time adoption is unchanged.

Four files: `src/scenes-34.js`, `scripts/epilogue-checks.mjs`,
`scripts/verify.mjs`, and this proof. Existing capacitor, Sela-answer,
living-cast and Tomas source/tests are unchanged and remain required by the
full verifier. No test is removed or weakened.

## Exact-head validation and limits

Final head/tree, independent read-only review, command results, three required
GitHub job URLs and the smoke repeat hash are recorded in the PR and V9 outbox.
Commands:

```sh
VERIFY_EXPECTED_SHA=<candidate> VERIFY_HEAD_REF=ticket/0.30.1-v035-playtest-epilogue-01 node scripts/verify.mjs
node scripts/verify.mjs --self-test
node scripts/release-policy.mjs --self-test
node scripts/simulate.mjs --self-test
node scripts/simulate.mjs --profile smoke --policy all --runs 64 --seed 20260817 --shard-size 32 --gate smoke
```

Automated tests use actual engine/rendered-button/import/resume code with
browser stubs and explicitly controlled saved contexts. They are not a fresh
end-to-end phone or newly deployed playtest. No hosted SHA is inferred from
the playtest report's visible version.

NO-PUBLISH / NOT_CERTIFIED; last certified remains 0.28.1d. L-002/L-021/L-024
truth protections remain; ART-R2 held, Amara-route parked, Netlify pin blocked.
No other leftover, Amara/Vess gate, pregnancy, Jiro, typo, opening backstory,
0.36, other game, merge, deployment or Orchestrator chat ping.

Next action: Manraj reviews this one PR and, if accepted, merge-commits in the
browser, not squash. Build stops for review and will not merge.
